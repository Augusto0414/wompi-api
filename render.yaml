services:
  - type: web
    name: wompi-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: node -r reflect-metadata dist/main
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService:
          type: web
          name: wompi-api
          property: port